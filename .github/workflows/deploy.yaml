name: Deploy changes to production
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull changes
        run: |
          cd ${{ secrets.PROJECT_REMOTE_PATH }}
          git pull origin main
          ls

